gsettings set com.canonical.Unity.Launcher launcher-position Bottom;sudo apt install gnome-tweak-tool -y;gsettings set org.gnome.shell.extensions.dash-to-dock click-action 'minimize';sudo apt-get install -y libxss1 libappindicator1 libindicator7;wget https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb; sudo dpkg -i google-chrome*.deb;sudo apt-get install default-jre -y;sudo apt-get install default-jdk -y;sudo apt-get install flashplugin-installer -y;sudo apt-get install ubuntu-restricted-extras -y;sudo apt install curl;curl -sL https://deb.nodesource.com/setup_8.x | sudo -E bash -;sudo apt-get install -y nodejs;
